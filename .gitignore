# Scratch and rendered outputs are regenerated by the scripts — don't track them.
work/
output/

# macOS
.DS_Store

# Don't commit trajectories — point config.sh at your own file instead.
*.vtf
*.dcd
*.xtc
*.trr
*.lammpstrj
